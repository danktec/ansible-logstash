output {

  file {
    path => "/tmp/stash.log"
  }

  elasticsearch {
    hosts => "{{ elasticsearch_address }}"
    user => "{{ elasticsearch_user }}"
    password => "{{ elasticsearch_pass }}"
  }

}
