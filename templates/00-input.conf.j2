input {
    rabbitmq {
        host => "{{ ansible_default_ipv4.address }}"
        user => "sensu"
        password => "sensu"
        vhost => "/sensu"
        exchange => "results"
        queue => "results_logging"
        key => ""
        durable => "true"
        type => "sensu_result"
    }
}
